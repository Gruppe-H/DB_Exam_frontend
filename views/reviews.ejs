<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= movie.primary_title %> - Reviews
    </title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <div class="container">
        <h1>
            <%= movie.primary_title %> (<%= movie.release_date %>)
        </h1>

        <% if (user) { %>
            <h2>Leave a Review:</h2>
            <%- include('reviewform', { movie: movie, user: user }) %>
            <% } else { %>
                <p><a href="/login">Login</a> to leave a review.</p>
                <% } %>

        <%- include('review', { movie: movie, user: user, reviews: reviews, from_movie_page: false }) %>
    </div>
</body>

</html>