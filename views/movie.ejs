<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= movie.primary_title %>
    </title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <div class="movie-container">
        <img src="/Images/<%= movie.id %>.jpg" alt="<%= movie.primary_title %>" class="movie-poster">
        <h1>
            <%= movie.primary_title %> (<%= movie.release_date %>)
        </h1>
        <p><strong>Director:</strong>
            <%= director ? director.actor.name : null %>

        </p>
        <p><strong>Duration:</strong>
            <%= movie.duration %>

        </p>
        <p><strong>Genre:</strong>
            <%= movie.genres %>
        </p>
        <p><strong>Rating:</strong>
            <%= movie.rating %>/100
        </p>
        <p>
            <%= movie.description %>
        </p>

        <h2>Crew:</h2>
        <% if (actors.length===0) { %>
            <p>No crew members found for this movie.</p>
            <% } else { %>
                <ul>
                    <% actors.forEach(actor=> { %>
                        <li><strong>
                                <%= actor.actor.name %>
                            </strong> - <%= actor.professions.join(', ') %></li>
                <% }); %>
            </ul>
        <% } %>

        <%- include('review', { movie: movie, user: user, reviews: reviews, from_movie_page: true }) %>
    </div>
</body>

</html>